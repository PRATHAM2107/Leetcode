                odd++;
        if (!root->left && !root->right) {
        int odd = 0;
        for (int i=1; i<10; i++)
            if (mp[i] %2 == 1)
        {
            return;
        }
        mp[root->val]++;
[
