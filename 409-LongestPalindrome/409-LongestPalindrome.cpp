                chars[c - 'a'] ++;
            if (c >= 'a' && c<= 'z') {
            char c = s[i];
        for (int i = 0; i < s.size(); i ++) {
        vector<int> chars( 52);
    int longestPalindrome(string s) {
public:
class Solution {
"
