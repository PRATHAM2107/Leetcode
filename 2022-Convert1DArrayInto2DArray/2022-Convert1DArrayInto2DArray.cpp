        for(int i=0; i<m && k<sz; i++)
        {
            for(int j=0; j<n && k<sz ; j++)
            {
                ans[i][j]= original[k++];
            }
        int k=0, sz=original.size();
        vector<vector<int>> ans(m, vector<int>(n, 0));
    vector<vector<int>> construct2DArray(vector<int>& original, int m, int n) {
public:
class Solution {
        if(m*n!=sz)  
            return {};
[1,2,3,4]
[
[
[
